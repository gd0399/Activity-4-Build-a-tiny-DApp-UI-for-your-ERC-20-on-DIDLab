{
  "name": "erc20-didlab",
  "version": "1.0.0",
  "scripts": {
    "test": "npx hardhat test",
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network didlab"
  },
  "devDependencies": {
    "hardhat": "2.19.1",
    "@nomicfoundation/hardhat-toolbox": "3.0.0",
    "ethers": "5.7.2",
    "dotenv": "^16.0.3",
    "typescript": "^5.3.3",
    "ts-node": "^10.9.2"
  }
}
